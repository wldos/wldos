# 脚本神器插件使用指南

## 📖 目录

1. [快速开始](#快速开始)
2. [核心概念](#核心概念)
3. [BitBrowser引擎使用](#bitbrowser引擎使用)
4. [脚本示例](#脚本示例)
5. [完整工作流程](#完整工作流程)
6. [常见问题](#常见问题)

---

## 🚀 快速开始

### 1. 前置准备

#### 1.1 安装比特指纹浏览器

1. 下载并安装[比特指纹浏览器](https://bitbrowser.cn/)
2. 启动比特浏览器，确保API服务正常运行（默认端口：54345）
3. 创建浏览器配置文件（Profile）

#### 1.2 配置插件

在WLDOS系统中配置比特浏览器API：

**方式1：通过配置文件（application.yml）**
```yaml
bitbrowser:
  api:
    url: http://127.0.0.1:54345
    key: your_api_key_here
    timeout: 30000
```

**方式2：通过数据库配置**
```java
// 使用AirdropConfigService设置配置
configService.setConfigValue("BITBROWSER", "api.url", "http://127.0.0.1:54345", "比特浏览器API地址", 1);
configService.setConfigValue("BITBROWSER", "api.key", "your_api_key", "API密钥", 2);
```

---

## 📚 核心概念

### 1. BitBrowser引擎

`BitBrowserEngine` 是插件的核心组件，负责：
- 启动和管理浏览器实例
- 执行脚本操作
- 批量任务调度
- 页面自动化操作

### 2. 脚本执行引擎

支持三种脚本类型：
- **JavaScript**: 使用Java内置引擎（Nashorn/GraalVM）
- **Python**: 调用系统Python解释器
- **Shell**: 支持Linux/Windows Shell脚本

### 3. 任务执行流程

```
创建任务 → 配置队列 → 启动浏览器 → 执行脚本 → 完成/失败
    ↓         ↓          ↓          ↓         ↓
  任务配置   并发控制   浏览器实例   脚本引擎   执行记录
```

---

## 🎯 BitBrowser引擎使用

### 1. 基本使用

#### 1.1 启动浏览器实例

```java
@Autowired
private BitBrowserEngine bitBrowserEngine;

// 启动单个浏览器实例
Map<String, Object> config = new HashMap<>();
config.put("profileId", "profile_001");
config.put("userAgent", "Mozilla/5.0...");
config.put("proxy", "http://proxy:8080");

BitBrowserEngine.BrowserInstance instance = 
    bitBrowserEngine.startBrowserInstance("profile_001", config);

if ("RUNNING".equals(instance.getStatus())) {
    System.out.println("浏览器启动成功");
}
```

#### 1.2 执行脚本

```java
// 在浏览器中执行JavaScript脚本
String scriptContent = 
    "console.log('Hello from browser!');" +
    "await page.goto('https://example.com');" +
    "await page.waitForTimeout(2000);";

Map<String, Object> result = bitBrowserEngine.executeScript(
    "profile_001", 
    scriptContent, 
    "javascript"
);

if ((Boolean) result.get("success")) {
    System.out.println("脚本执行成功: " + result.get("result"));
}
```

#### 1.3 批量执行

```java
// 批量执行脚本（多个浏览器实例）
List<String> profileIds = Arrays.asList(
    "profile_001", 
    "profile_002", 
    "profile_003"
);

Map<String, Object> queueConfig = new HashMap<>();
queueConfig.put("concurrency", 3);      // 并发数
queueConfig.put("taskInterval", 2);    // 任务间隔（秒）
queueConfig.put("errorRetry", 3);      // 错误重试次数

Map<String, Object> result = bitBrowserEngine.executeBatchScripts(
    profileIds,
    scriptContent,
    "javascript",
    queueConfig
);
```

#### 1.4 页面操作

```java
// 导航到页面
Map<String, Object> navigateParams = new HashMap<>();
navigateParams.put("url", "https://example.com");
bitBrowserEngine.executePageAction("profile_001", "navigate", navigateParams);

// 点击元素
Map<String, Object> clickParams = new HashMap<>();
clickParams.put("selector", "button#submit");
bitBrowserEngine.executePageAction("profile_001", "click", clickParams);

// 输入文本
Map<String, Object> inputParams = new HashMap<>();
inputParams.put("selector", "input[name='username']");
inputParams.put("value", "testuser");
bitBrowserEngine.executePageAction("profile_001", "input", inputParams);
```

---

## 📝 脚本示例

### 1. JavaScript脚本示例

#### 示例1：简单的页面操作

```javascript
// 访问网站并执行操作
console.log('开始执行任务');

// 1. 访问目标网站
await page.goto('https://linera.io');
await page.waitForTimeout(2000);

// 2. 查找并点击按钮
const connectButton = await page.$('[data-testid="connect-wallet"]');
if (connectButton) {
    await connectButton.click();
    await page.waitForTimeout(1000);
    console.log('钱包连接成功');
}

// 3. 填写表单
const input = await page.$('input[name="email"]');
if (input) {
    await input.type('test@example.com');
}

// 4. 提交表单
const submitButton = await page.$('button[type="submit"]');
if (submitButton) {
    await submitButton.click();
    await page.waitForTimeout(2000);
}

console.log('任务完成');
```

#### 示例2：使用上下文变量

```javascript
// 使用从Java传入的上下文变量
console.log('用户名:', username);
console.log('邮箱:', email);

// 使用变量填充表单
await page.fill('input[name="username"]', username);
await page.fill('input[name="email"]', email);

// 执行操作
await page.click('button[type="submit"]');
```

#### 示例3：处理异步操作

```javascript
// 等待元素出现
await page.waitForSelector('.loading', { state: 'hidden', timeout: 10000 });

// 等待网络请求完成
await page.waitForResponse(response => 
    response.url().includes('/api/complete') && response.status() === 200
);

// 等待特定时间
await page.waitForTimeout(3000);
```

### 2. Python脚本示例

#### 示例1：数据处理

```python
# Python脚本示例
import json
import time

print("开始执行Python任务")

# 使用上下文变量
print(f"任务名称: {taskName}")
print(f"用户ID: {userId}")

# 数据处理
data = {
    "timestamp": int(time.time()),
    "task": taskName,
    "user": userId
}

# 输出结果
result = json.dumps(data)
print(f"处理结果: {result}")
```

#### 示例2：API调用

```python
# 使用Python进行API调用
import requests
import json

# 调用API
response = requests.post(
    'https://api.example.com/claim',
    json={
        'wallet': walletAddress,
        'network': network
    },
    headers={'Authorization': f'Bearer {apiKey}'}
)

if response.status_code == 200:
    result = response.json()
    print(f"Claim成功: {result}")
else:
    print(f"Claim失败: {response.text}")
```

### 3. Shell脚本示例

#### 示例1：文件操作

```bash
#!/bin/bash
# Shell脚本示例

echo "开始执行Shell任务"

# 创建目录
mkdir -p /tmp/airdrop_results

# 写入文件
echo "任务ID: $taskId" > /tmp/airdrop_results/task.log
echo "执行时间: $(date)" >> /tmp/airdrop_results/task.log

# 执行命令
curl -X POST https://api.example.com/notify \
  -H "Content-Type: application/json" \
  -d "{\"taskId\": \"$taskId\", \"status\": \"completed\"}"

echo "任务完成"
```

---

## 🔄 完整工作流程

### 场景：批量执行Linera空投任务

#### 步骤1：准备浏览器配置文件

在比特浏览器中创建多个配置文件，每个配置对应一个账号：
- `profile_001` - 账号1
- `profile_002` - 账号2
- `profile_003` - 账号3
- ...

#### 步骤2：创建任务配置

```java
@Autowired
private AirdropScheduler airdropScheduler;

// 创建批量任务
Map<String, Object> taskConfig = new HashMap<>();
taskConfig.put("name", "Linera批量空投任务");
taskConfig.put("projectType", "linera");
taskConfig.put("scriptType", "javascript");

// 脚本内容（或使用模板）
taskConfig.put("scriptContent", 
    "// Linera空投脚本\n" +
    "await page.goto('https://linera.io');\n" +
    "await page.waitForTimeout(2000);\n" +
    "// 连接钱包\n" +
    "const walletButton = await page.$('[data-testid=\"connect-wallet\"]');\n" +
    "if (walletButton) {\n" +
    "    await walletButton.click();\n" +
    "    await page.waitForTimeout(1000);\n" +
    "}\n" +
    "// 关注Twitter\n" +
    "await followTwitter('@linera_official');\n" +
    "// 加入Discord\n" +
    "await joinDiscord('linera');\n" +
    "console.log('任务完成');"
);

// 浏览器配置文件ID列表
List<String> profileIds = Arrays.asList(
    "profile_001", "profile_002", "profile_003"
);
taskConfig.put("profileIds", profileIds);

// 队列配置
Map<String, Object> queueConfig = new HashMap<>();
queueConfig.put("concurrency", 3);        // 同时执行3个任务
queueConfig.put("taskInterval", 2);       // 任务间隔2秒
queueConfig.put("errorRetry", 3);         // 失败重试3次
queueConfig.put("queueRepeat", 1);       // 队列重复1次

// 附带动作（可选）
List<Map<String, Object>> attachedActions = new ArrayList<>();
Map<String, Object> twitterAction = new HashMap<>();
twitterAction.put("type", "TWITTER_BIND");
twitterAction.put("name", "绑定Twitter");
twitterAction.put("timing", "PRE");
twitterAction.put("enabled", true);
twitterAction.put("params", Map.of(
    "account", "twitter_account",
    "password", "twitter_password"
));
attachedActions.add(twitterAction);

Map<String, Object> claimAction = new HashMap<>();
claimAction.put("type", "AUTO_CLAIM");
claimAction.put("name", "自动Claim");
claimAction.put("timing", "POST");
claimAction.put("enabled", true);
claimAction.put("params", Map.of(
    "walletAddress", "0x123...",
    "network", "ethereum"
));
attachedActions.add(claimAction);

queueConfig.put("attachedActions", attachedActions);
taskConfig.put("queueConfig", queueConfig);
```

#### 步骤3：创建并执行任务

```java
// 创建调度任务
String taskId = airdropScheduler.createBatchTask(taskConfig);
System.out.println("任务创建成功，ID: " + taskId);

// 执行任务
Map<String, Object> executeResult = airdropScheduler.executeBatchTask(taskId);
System.out.println("执行结果: " + executeResult);
```

#### 步骤4：监控任务执行

```java
// 查询任务状态
AirdropScheduler.ScheduledTask task = airdropScheduler.getScheduledTask(taskId);
System.out.println("任务状态: " + task.getStatus());
System.out.println("完成数: " + task.getCompletedTasks());
System.out.println("失败数: " + task.getFailedTasks());

// 获取执行记录
List<AirdropExecution> executions = executionService.findByTaskId(taskId);
for (AirdropExecution execution : executions) {
    System.out.println("执行ID: " + execution.getId());
    System.out.println("状态: " + execution.getStatus());
    System.out.println("执行时间: " + execution.getDuration());
}
```

---

## 🎬 实际应用场景

### 场景1：Linera空投任务

**目标**: 批量完成Linera空投任务

**步骤**:
1. 访问Linera官网
2. 连接钱包
3. 关注Twitter账号
4. 加入Discord
5. 完成测试网交互
6. 自动Claim奖励

**脚本示例**:
```javascript
// Linera空投完整脚本
console.log('开始Linera空投任务');

// 1. 访问官网
await page.goto('https://linera.io');
await page.waitForTimeout(3000);

// 2. 连接钱包
const connectBtn = await page.$('[data-testid="connect-wallet"]');
if (connectBtn) {
    await connectBtn.click();
    await page.waitForTimeout(2000);
    
    // 选择钱包类型（如MetaMask）
    const metamaskBtn = await page.$('button:has-text("MetaMask")');
    if (metamaskBtn) {
        await metamaskBtn.click();
        await page.waitForTimeout(3000);
    }
}

// 3. 关注Twitter
await page.goto('https://twitter.com/linera_official');
await page.waitForTimeout(2000);
const followBtn = await page.$('[data-testid="follow"]');
if (followBtn) {
    await followBtn.click();
    await page.waitForTimeout(1000);
}

// 4. 加入Discord
await page.goto('https://discord.gg/linera');
await page.waitForTimeout(2000);
// Discord加入逻辑...

// 5. 完成测试网任务
await page.goto('https://linera.io/testnet');
await page.waitForTimeout(2000);
// 执行测试网交互...

// 6. Claim奖励
const claimBtn = await page.$('button:has-text("Claim")');
if (claimBtn) {
    await claimBtn.click();
    await page.waitForTimeout(3000);
}

console.log('Linera空投任务完成');
```

### 场景2：Movement测试网交互

**目标**: 批量完成Movement测试网交互任务

**脚本示例**:
```javascript
// Movement测试网交互脚本
console.log('开始Movement测试网交互');

// 1. 访问Movement官网
await page.goto('https://movement.network');
await page.waitForTimeout(2000);

// 2. 连接钱包
await connectWallet();

// 3. 领取测试币
await claimTestTokens();

// 4. 执行Swap
await performSwap('ETH', 'USDC', '0.01');

// 5. 提供流动性
await provideLiquidity('ETH/USDC', '0.005');

// 6. 质押代币
await stakeTokens('MOVEMENT', '100');

console.log('Movement测试网交互完成');
```

### 场景3：Polymarket交易任务

**目标**: 批量在Polymarket上执行交易

**脚本示例**:
```javascript
// Polymarket交易脚本
console.log('开始Polymarket交易');

// 1. 访问Polymarket
await page.goto('https://polymarket.com');
await page.waitForTimeout(2000);

// 2. 连接钱包
await connectWallet();

// 3. 搜索市场
await page.fill('input[placeholder="Search markets"]', 'Bitcoin Price');
await page.waitForTimeout(1000);

// 4. 选择市场
const market = await page.$('.market-card:first-child');
if (market) {
    await market.click();
    await page.waitForTimeout(2000);
}

// 5. 下注
const betButton = await page.$('button:has-text("Place Bet")');
if (betButton) {
    await betButton.click();
    await page.fill('input[name="amount"]', '10');
    await page.click('button[type="submit"]');
    await page.waitForTimeout(3000);
}

console.log('Polymarket交易完成');
```

---

## 🔧 高级功能

### 1. 使用附带动作

```java
// 在任务执行前自动绑定Twitter
AttachedAction twitterAction = new AttachedAction();
twitterAction.setType(AttachedAction.Type.TWITTER_BIND);
twitterAction.setName("绑定Twitter");
twitterAction.setTiming(AttachedAction.Timing.PRE);
twitterAction.setEnabled(true);
twitterAction.setParams(Map.of(
    "account", "twitter_account",
    "password", "twitter_password",
    "profileId", "profile_001"
));

ActionExecutionResult result = attachedActionExecutor.execute(twitterAction);
```

### 2. 使用自定义RPC

```java
// 配置自定义RPC节点
AttachedAction rpcAction = new AttachedAction();
rpcAction.setType(AttachedAction.Type.CUSTOM_RPC);
rpcAction.setParams(Map.of(
    "rpcUrl", "https://rpc.example.com",
    "network", "ethereum",
    "chainId", "1",
    "testConnection", true
));

ActionExecutionResult result = attachedActionExecutor.execute(rpcAction);
```

### 3. 自动Claim

```java
// 自动Claim奖励
AttachedAction claimAction = new AttachedAction();
claimAction.setType(AttachedAction.Type.AUTO_CLAIM);
claimAction.setTiming(AttachedAction.Timing.POST);
claimAction.setParams(Map.of(
    "walletAddress", "0x123...",
    "network", "ethereum",
    "profileId", "profile_001",
    "claimUrl", "https://example.com/claim"
));

ActionExecutionResult result = attachedActionExecutor.execute(claimAction);
```

---

## ❓ 常见问题

### Q1: 如何获取浏览器配置文件ID？

**A**: 在比特浏览器中：
1. 打开比特浏览器
2. 查看配置文件列表
3. 每个配置文件都有一个唯一的ID
4. 也可以通过API获取：`bitBrowserEngine.getBrowserList()`

### Q2: 脚本执行失败怎么办？

**A**: 检查以下几点：
1. 浏览器实例是否正常启动
2. 脚本语法是否正确
3. 页面元素选择器是否正确
4. 网络连接是否正常
5. 查看执行记录中的错误信息

### Q3: 如何调试脚本？

**A**: 
1. 在脚本中使用 `console.log()` 输出调试信息
2. 查看执行记录中的输出信息
3. 使用浏览器开发者工具手动测试脚本
4. 逐步执行脚本，检查每一步的结果

### Q4: 如何提高执行成功率？

**A**:
1. 增加任务间隔时间，避免请求过快
2. 使用随机延迟，模拟人工操作
3. 配置错误重试机制
4. 使用高质量的代理IP
5. 确保浏览器配置文件设置正确

### Q5: 支持哪些浏览器自动化操作？

**A**: 目前支持：
- 页面导航（navigate）
- 元素点击（click）
- 文本输入（input）
- 元素查找（checkElement）
- 脚本执行（execute）

更多操作可以通过脚本直接实现。

---

## 📞 技术支持

如有问题，请参考：
- [插件README](../README.md)
- [实现完成总结](../实现完成总结.md)
- [API文档](#api文档)

---

**文档版本**: v1.0  
**最后更新**: 2025-12-28

